{
    "builds": [{
        "src": "Forex_endpoint/wsgi.py",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "Forex_endpoint/wsgi.py"
        }
    ]
}